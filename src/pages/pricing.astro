---
import DefaultLayout from '@/layouts/DefaultLayout.astro';
import { Background } from '@/components/background';
import { HeroGlow } from '@/components/hero-glow';
import { breadcrumbList } from '@/lib/seo';
import { SITE_URL } from '@/consts';

const breadcrumb = breadcrumbList([
  { name: 'Home', path: '/' },
  { name: 'Pricing', path: '/pricing' },
]);

const offerCatalog = {
  "@context": "https://schema.org",
  "@type": "Service",
  name: "Hooklyne",
  serviceType: "B2B prospect research",
  provider: { "@type": "Organization", name: "Hooklyne", url: SITE_URL },
  areaServed: ["NL", "BE", "GB", "LU", "DE", "FR", "IE", "DK", "SE", "NO", "FI"],
  audience: { "@type": "BusinessAudience", audienceType: "B2B SME sales teams (10-100 people)" },
  hasOfferCatalog: {
    "@type": "OfferCatalog",
    name: "Hooklyne plans",
    itemListElement: [
      { "@type": "Offer", name: "Start", price: "39", priceCurrency: "EUR", url: `${SITE_URL}/pricing`, category: "annual billing" },
      { "@type": "Offer", name: "Growth", price: "129", priceCurrency: "EUR", url: `${SITE_URL}/pricing`, category: "annual billing" },
      { "@type": "Offer", name: "Scale", price: "239", priceCurrency: "EUR", url: `${SITE_URL}/pricing`, category: "annual billing" },
      { "@type": "Offer", name: "Enterprise", url: `${SITE_URL}/contact`, category: "custom" },
    ],
  },
};
---

<DefaultLayout title="Pricing | Hooklyne" description="Pay for what you use. Every credit is a clear deliverable. Start, Growth, Scale, and Enterprise plans." image="/og/EN/pricing.jpg" ctaVariant="pricing">
  <script type="application/ld+json" is:inline set:html={JSON.stringify(breadcrumb)} />
  <script type="application/ld+json" is:inline set:html={JSON.stringify(offerCatalog)} />
  <Background>
  <section class="pt-24 sm:pt-32 lg:pt-44 pb-10 relative">
    <HeroGlow client:idle signals={false} />
    <div id="hero-content" class="container max-w-6xl relative z-10">

      <!-- Header -->
      <div class="text-center max-w-2xl mx-auto">
        <p class="text-xs font-semibold uppercase tracking-widest text-[var(--hooklyne-blue)] mb-4">Pricing</p>
        <h1 class="hero-headline text-4xl md:text-5xl lg:text-6xl tracking-tight text-[var(--heading)] leading-[1.1] mb-5">Simple pricing with <span class="text-accent">transparent credits</span>.</h1>
        <p class="text-lg text-[var(--muted-foreground)] max-w-2xl mx-auto leading-relaxed">One credit system. Every action priced the same across plans. No per-seat fees on Start and Growth. Switch plans or cancel anytime.</p>
        <a href="/how-it-works" class="inline-block mt-3 text-[12.5px] text-[var(--muted-foreground)] hover:text-[var(--hooklyne-blue)] hover:underline transition-colors">
          How we are different from other prospecting tools →
        </a>
      </div>

      <!-- Free trial banner -->
      <div class="mt-12 max-w-3xl mx-auto rounded-2xl px-5 py-4 sm:px-6 sm:py-5 flex flex-wrap items-center justify-between gap-4" style="background: var(--card); border: 1px solid rgba(13,148,136,0.30); box-shadow: var(--shadow-xs)">
        <div class="flex-1 min-w-[260px]">
          <p class="text-[10px] font-semibold uppercase tracking-[0.18em] mb-1" style="color: var(--hooklyne-teal)">Free trial · No card</p>
          <p class="text-[14px] text-[var(--heading)] leading-relaxed">
            <span class="font-semibold">10 credits free.</span> Real prospects, in your voice, in NL or EN. Test before you pay.
          </p>
        </div>
        <a href="/contact" data-track="pricing_trial_cta" class="inline-flex items-center justify-center gap-1.5 rounded-lg px-5 py-2.5 text-[13px] font-semibold text-white transition-opacity hover:opacity-90 btn-shine whitespace-nowrap" style="background: var(--hooklyne-teal)">
          Try free
          <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M5 12h14"/><path d="m12 5 7 7-7 7"/></svg>
        </a>
      </div>

    </div>
  </section>

  <section class="pb-20">
    <div class="container max-w-6xl">

      <!-- Toggle -->
      <div class="flex items-center justify-center gap-3 mb-10">
        <span class="text-sm font-medium transition-colors" id="label-monthly" style="color: var(--muted-foreground)">Monthly</span>
        <button
          id="toggle-btn"
          class="relative w-12 h-6 rounded-full transition-colors cursor-pointer btn-shine"
          style="background: var(--hooklyne-navy)"
          onclick="toggleBilling()"
          aria-label="Toggle billing period"
        >
          <span id="toggle-knob" class="absolute top-0.5 left-0.5 size-5 rounded-full bg-white transition-transform duration-200 shadow-sm" style="transform: translateX(24px)"></span>
        </button>
        <span class="text-sm font-medium flex items-center gap-2 transition-colors" id="label-annual" style="color: var(--heading)">
          Annual
          <span class="text-xs font-semibold px-2 py-0.5 rounded-full" style="background: rgba(13,148,136,0.10); color: var(--hooklyne-teal)">Save up to €60/mo</span>
        </span>
      </div>

      <!-- Plan cards -->
      <div class="grid gap-4 md:grid-cols-2 lg:grid-cols-4 mb-12">

        <!-- Start -->
        <div class="rounded-2xl p-5 flex flex-col relative" style="background: var(--card); box-shadow: var(--shadow-xs)">
          <p class="text-xs font-semibold uppercase tracking-widest text-[var(--muted-foreground)] mb-3 mt-1">Start</p>
          <div class="mb-1">
            <span class="text-3xl font-bold text-[var(--heading)] price-display" data-monthly="49" data-annual="39">€39</span>
            <span class="text-sm text-[var(--muted-foreground)] ml-1">/mo</span>
          </div>
          <div class="h-5 mb-2">
            <span class="text-xs font-medium save-note text-[var(--hooklyne-teal)]">Save €10/mo on annual</span>
          </div>
          <p class="text-xs leading-relaxed text-[var(--muted-foreground)] mb-4">Solo rep. Test and validate your outbound. Self-serve.</p>
          <div class="rounded-xl px-3 py-2.5 mb-4" style="background: var(--background)">
            <div class="flex items-baseline gap-2">
              <span class="text-2xl font-bold text-[var(--hooklyne-blue)]">100</span>
              <span class="text-xs text-[var(--muted-foreground)]">credits / month</span>
            </div>
            <p class="text-[10.5px] text-[var(--muted-foreground)] leading-snug mt-1.5">≈ 25 ready-to-send prospects, 10 tracked companies, 5 meeting briefs</p>
          </div>
          <ul class="flex-1 space-y-2 mb-5">
            {["1 seat", "Find companies + contacts", "Outreach in NL + EN", "Weekly market briefing", "Meeting prep briefs", "Signal tracking: up to 10 companies"].map((f) => (
              <li class="flex items-start gap-2 text-xs text-[var(--muted-foreground)]">
                <svg class="size-3.5 shrink-0 mt-0.5 text-[var(--hooklyne-teal)]" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M20 6 9 17l-5-5"/></svg>
                {f}
              </li>
            ))}
          </ul>
          <p class="text-xs text-[var(--muted-foreground)]/60 mb-4">Top-up: €0.85/cr</p>
          <a href="/contact" data-tier="start" data-location="pricing" class="block text-center rounded-lg text-sm font-semibold py-2.5 transition-all btn-shine ring-1 ring-[var(--hooklyne-navy)] text-[var(--heading)] hover:bg-[var(--hooklyne-navy)] hover:text-white">
            Get started
          </a>
        </div>

        <!-- Growth (featured) -->
        <div class="rounded-2xl p-5 flex flex-col relative text-white" style="background: var(--hooklyne-navy); box-shadow: var(--shadow-xl)">
          <div class="absolute -top-3 left-1/2 -translate-x-1/2 text-white text-xs font-semibold px-3 py-1 rounded-full whitespace-nowrap" style="background: var(--hooklyne-orange)">
            Recommended
          </div>
          <p class="text-xs font-semibold uppercase tracking-widest text-white/40 mb-3 mt-1">Growth</p>
          <div class="mb-1">
            <span class="text-3xl font-bold text-white price-display" data-monthly="159" data-annual="129">€129</span>
            <span class="text-sm text-white/40 ml-1">/mo</span>
          </div>
          <div class="h-5 mb-2">
            <span class="text-xs font-medium save-note text-green-300">Save €30/mo on annual</span>
          </div>
          <p class="text-xs leading-relaxed text-white/50 mb-4">1-2 reps. Full pipeline. Setup call included.</p>
          <div class="rounded-xl px-3 py-2.5 mb-4" style="background: rgba(255,255,255,0.08)">
            <div class="flex items-baseline gap-2">
              <span class="text-2xl font-bold text-blue-300">400</span>
              <span class="text-xs text-white/40">credits / month</span>
            </div>
            <p class="text-[10.5px] text-white/55 leading-snug mt-1.5">≈ 90 ready-to-send prospects, 30 tracked companies, 10 meeting briefs</p>
          </div>
          <ul class="flex-1 space-y-2 mb-5">
            {["2 seats (add up to 2 extra at €30/seat/mo)", "Find companies + contacts", "Outreach in NL + EN", "Weekly market briefing", "Meeting prep briefs", "Signal tracking: up to 40 companies", "45-min setup + strategy call", "ICP configured with you", "Monthly check-in (first 3 months)"].map((f) => (
              <li class="flex items-start gap-2 text-xs text-white/70">
                <svg class="size-3.5 shrink-0 mt-0.5 text-green-300" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M20 6 9 17l-5-5"/></svg>
                {f}
              </li>
            ))}
          </ul>
          <p class="text-xs text-white/30 mb-4">Top-up: €0.85/cr</p>
          <a href="/contact" data-tier="growth" data-location="pricing" class="block text-center rounded-lg text-sm font-semibold py-2.5 transition-all btn-shine bg-white/14 text-white border border-white/22 hover:bg-white/24">
            Get started
          </a>
        </div>

        <!-- Scale -->
        <div class="rounded-2xl p-5 flex flex-col relative" style="background: var(--card); box-shadow: var(--shadow-xs)">
          <p class="text-xs font-semibold uppercase tracking-widest text-[var(--muted-foreground)] mb-3 mt-1">Scale</p>
          <div class="mb-1">
            <span class="text-3xl font-bold text-[var(--heading)] price-display" data-monthly="299" data-annual="239">€239</span>
            <span class="text-sm text-[var(--muted-foreground)] ml-1">/mo</span>
          </div>
          <div class="h-5 mb-2">
            <span class="text-xs font-medium save-note text-[var(--hooklyne-teal)]">Save €60/mo on annual</span>
          </div>
          <p class="text-xs leading-relaxed text-[var(--muted-foreground)] mb-4">Small sales team. Volume outbound. Up to 5 reps.</p>
          <div class="rounded-xl px-3 py-2.5 mb-4" style="background: var(--background)">
            <div class="flex items-baseline gap-2">
              <span class="text-2xl font-bold text-[var(--hooklyne-blue)]">800</span>
              <span class="text-xs text-[var(--muted-foreground)]">credits / month</span>
            </div>
            <p class="text-[10.5px] text-[var(--muted-foreground)] leading-snug mt-1.5">≈ 190 ready-to-send prospects, 60 tracked companies, 20 meeting briefs</p>
          </div>
          <ul class="flex-1 space-y-2 mb-5">
            {["5 seats", "Find companies + contacts", "Outreach in NL + EN", "Weekly market briefing", "Meeting prep briefs", "Signal tracking: up to 100 companies", "Full setup + team training", "Dedicated Slack or WhatsApp", "Bi-weekly check-in calls", "Monthly playbook iteration"].map((f) => (
              <li class="flex items-start gap-2 text-xs text-[var(--muted-foreground)]">
                <svg class="size-3.5 shrink-0 mt-0.5 text-[var(--hooklyne-teal)]" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M20 6 9 17l-5-5"/></svg>
                {f}
              </li>
            ))}
          </ul>
          <p class="text-xs text-[var(--muted-foreground)]/60 mb-4">Top-up: €0.85/cr</p>
          <a href="/contact" data-tier="scale" data-location="pricing" class="block text-center rounded-lg text-sm font-semibold py-2.5 transition-all btn-shine ring-1 ring-[var(--hooklyne-navy)] text-[var(--heading)] hover:bg-[var(--hooklyne-navy)] hover:text-white">
            Get started
          </a>
        </div>

        <!-- Enterprise -->
        <div class="rounded-2xl p-5 flex flex-col relative" style="background: var(--card); box-shadow: var(--shadow-xs)">
          <p class="text-xs font-semibold uppercase tracking-widest text-[var(--muted-foreground)] mb-3 mt-1">Enterprise</p>
          <div class="mb-1">
            <span class="text-3xl font-bold text-[var(--heading)]">Custom</span>
          </div>
          <div class="h-5 mb-2"></div>
          <p class="text-xs leading-relaxed text-[var(--muted-foreground)] mb-4">Sales departments. Multi-market. Custom volumes.</p>
          <div class="rounded-xl px-3 py-2.5 mb-4" style="background: var(--background)">
            <div class="flex items-baseline gap-2">
              <span class="text-2xl font-bold text-[var(--hooklyne-blue)]">Custom</span>
              <span class="text-xs text-[var(--muted-foreground)]">credit volume</span>
            </div>
            <p class="text-[10.5px] text-[var(--muted-foreground)] leading-snug mt-1.5">Negotiated to your team's outbound volume</p>
          </div>
          <ul class="flex-1 space-y-2 mb-5">
            {["Unlimited seats", "Find companies + contacts", "Outreach in NL + EN", "Weekly market briefing", "Meeting prep briefs", "Signal tracking: negotiated", "Full setup + team training", "Dedicated account manager"].map((f) => (
              <li class="flex items-start gap-2 text-xs text-[var(--muted-foreground)]">
                <svg class="size-3.5 shrink-0 mt-0.5 text-[var(--hooklyne-teal)]" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M20 6 9 17l-5-5"/></svg>
                {f}
              </li>
            ))}
          </ul>
          <p class="text-xs text-[var(--muted-foreground)]/60 mb-4">Top-up: Custom rate</p>
          <a href="/contact" data-tier="enterprise" data-location="pricing" class="block text-center rounded-lg text-sm font-semibold py-2.5 transition-all btn-shine ring-1 ring-[var(--hooklyne-navy)] text-[var(--heading)] hover:bg-[var(--hooklyne-navy)] hover:text-white">
            Talk to us
          </a>
        </div>

      </div>

      <!-- Subnote under plan cards -->
      <p class="text-center text-xs text-[var(--muted-foreground)] leading-relaxed max-w-3xl mx-auto mb-20 -mt-6">
        Annual plans billed upfront. Credits valid 12 months. Unused credits roll 1 month forward (max balance 2× your monthly quota). Top-up at €0.85/cr on all plans. Signal tracking cap is per workspace, not per user. Growth extra seats: +€30/seat/mo, up to 4 seats total.
      </p>

      <!-- ===========================================================
           HOW CREDITS WORK - lifecycle structure
           =========================================================== -->
      <div class="mb-16">
        <div class="max-w-3xl mb-10">
          <h2 class="text-2xl md:text-3xl font-semibold text-[var(--heading)] tracking-tight mb-3">How credits work</h2>
          <p class="text-base text-[var(--muted-foreground)] leading-relaxed">
            Credits are spent when you produce something - a prospect, a contact, a brief. Browsing, reading signals, viewing intel, and exporting are always free. The flow below mirrors how prospects move through your pipeline.
          </p>
        </div>

        <!-- ─── STAGE 1 ─── -->
        <div class="flex items-center gap-3.5 mb-5">
          <span class="flex items-center justify-center size-9 rounded-full text-base font-bold text-white shrink-0" style="background: var(--hooklyne-navy)">1</span>
          <div>
            <p class="text-[15px] font-semibold text-[var(--heading)] leading-tight">Get prospects into My Leads</p>
            <p class="text-[12.5px] text-[var(--muted-foreground)] leading-snug mt-0.5">Pick the path that matches what you already know. Every path ends in a ready-to-send prospect package in My Leads.</p>
          </div>
        </div>

        <div class="grid gap-3 sm:grid-cols-2 lg:grid-cols-4 mb-5">
          {[
            { n: "1", title: "Find me companies", featured: true, desc: "Describe your ICP. Hooklyne returns up to 20 ranked companies with up to 3 matched roles each.", lines: [["Discovery (up to 20 companies)", "1cr"], ["Prospect package", "1cr"], ["Contact (email + company phone)", "2cr"]], total: "4cr", soon: false },
            { n: "2", title: "I know the company", featured: false, desc: "Enter a company name and domain. We surface up to 3 matched roles for you to pick from.", lines: [["View matched roles", "Free"], ["Prospect package", "1cr"], ["Contact (email + company phone)", "2cr"]], total: "3cr", soon: false },
            { n: "3", title: "I know who to reach", featured: false, desc: "You have a name and LinkedIn URL. We verify the email and write outreach in your voice.", lines: [["Prospect package", "1cr"], ["Contact (email + company phone)", "2cr"]], total: "3cr", soon: false },
            { n: "4", title: "Find similar companies", featured: false, desc: "Upload your best customers. Hooklyne finds companies that look just like them - same size, sector, and signals.", lines: [["ICP modelling", "TBA"], ["Prospect package", "TBA"], ["Contact (email + company phone)", "TBA"]], total: "TBA", soon: true },
          ].map((c) => (
            <div class:list={["relative rounded-2xl p-5 flex flex-col", c.featured ? "border-2" : "border", c.soon && "border-dashed"]} style={c.featured ? "background: var(--card); border-color: var(--hooklyne-blue); box-shadow: var(--shadow-sm)" : c.soon ? "background: #f8fafc; border-color: var(--border-strong)" : "background: var(--card); border-color: var(--border); box-shadow: var(--shadow-xs)"}>
              {c.soon && <span class="absolute top-3 right-3 text-[9.5px] font-semibold uppercase tracking-[0.12em] text-[var(--muted-foreground)] px-2 py-0.5 rounded-full bg-white border border-[var(--border)]">Coming soon</span>}
              <div class="flex items-center gap-2.5 mb-3">
                <span class:list={["flex items-center justify-center size-6 rounded-full text-[11px] font-bold text-white shrink-0"]} style={c.featured ? "background: var(--hooklyne-blue)" : c.soon ? "background: #cbd5e1" : "background: var(--muted-foreground)"}>
                  {c.n}
                </span>
                <span class="text-[13px] font-semibold text-[var(--heading)]">{c.title}</span>
              </div>
              <p class="text-[12px] text-[var(--muted-foreground)] leading-relaxed mb-4 min-h-[58px]">{c.desc}</p>
              <div class="border-t border-[var(--border)] pt-2.5 mt-auto space-y-1">
                {c.lines.map(([k, v]) => (
                  <div class="flex items-baseline justify-between text-[11.5px]">
                    <span class="text-[var(--muted-foreground)]">{k}</span>
                    <span class:list={[v === "Free" ? "text-[var(--hooklyne-teal)] italic" : v === "TBA" ? "text-[var(--muted-foreground)] italic" : "text-[var(--hooklyne-blue)] font-semibold"]}>{v}</span>
                  </div>
                ))}
                <div class="flex items-baseline justify-between pt-1.5 mt-1.5 border-t border-[var(--hooklyne-navy)]/15">
                  <span class="text-[11.5px] font-semibold text-[var(--heading)]">Per prospect</span>
                  <span class:list={["text-[14px] font-semibold", c.total === "TBA" ? "text-[var(--muted-foreground)]" : "text-[var(--hooklyne-navy)]"]}>{c.total}</span>
                </div>
              </div>
            </div>
          ))}
        </div>

        <p class="text-[13px] text-[var(--muted-foreground)] leading-relaxed mb-3 max-w-3xl">
          <span class="font-semibold text-[var(--heading)]">In practice:</span> one discovery (1cr) returns up to 20 ranked companies. Build the 5 best fits = <span class="font-semibold text-[var(--heading)]">16cr for 5 ready-to-send prospects</span>. Build all 20 = 61cr. Skip a company that does not fit: no extra cost.
        </p>
        <p class="text-[12px] text-[var(--muted-foreground)] leading-relaxed mb-12">
          <span class="text-[var(--hooklyne-blue)] font-semibold">Coming soon ·</span> Direct dial / mobile, 3cr per number found. Use credits from any plan to add a verified personal phone. No find, no charge.
        </p>

        <!-- ─── STAGE 2 ─── -->
        <div class="flex items-center gap-3.5 mb-5">
          <span class="flex items-center justify-center size-9 rounded-full text-base font-bold text-white shrink-0" style="background: var(--hooklyne-navy)">2</span>
          <div>
            <p class="text-[15px] font-semibold text-[var(--heading)] leading-tight">Work prospects in My Leads</p>
            <p class="text-[12.5px] text-[var(--muted-foreground)] leading-snug mt-0.5">Once a prospect is in your pipeline, you can track buying signals on them and request a meeting prep brief before any conversation.</p>
          </div>
        </div>

        <div class="grid gap-3 md:grid-cols-2 mb-12">
          <div class="rounded-2xl p-5 flex gap-4 items-start" style="background: var(--card); border: 1px solid var(--border); box-shadow: var(--shadow-xs)">
            <span class="inline-flex items-center justify-center min-w-12 h-11 rounded-lg text-base font-semibold shrink-0" style="background: rgba(52,76,163,0.10); color: var(--hooklyne-blue)">2cr</span>
            <div class="flex-1 min-w-0">
              <div class="flex items-baseline justify-between gap-3 mb-1">
                <span class="text-[13.5px] font-semibold text-[var(--heading)]">Signal tracking</span>
                <span class="text-[10.5px] text-[var(--muted-foreground)] whitespace-nowrap">2cr / company / month</span>
              </div>
              <p class="text-[12px] text-[var(--muted-foreground)] leading-relaxed mb-3"><span class="font-semibold text-[var(--heading)]">Daily sweeps for ~€0.02/day.</span> Job posts, news, LinkedIn activity, funding. Scored and ranked. When a hot signal fires, the outreach is already drafted.</p>
              <div class="text-[11px] text-[var(--muted-foreground)] pt-2 border-t border-[var(--border)] flex flex-wrap gap-x-4 gap-y-1">
                <span><strong class="text-[var(--heading)] font-semibold">Start</strong> up to 10</span>
                <span><strong class="text-[var(--heading)] font-semibold">Growth</strong> up to 40</span>
                <span><strong class="text-[var(--heading)] font-semibold">Scale</strong> up to 100</span>
              </div>
            </div>
          </div>
          <div class="rounded-2xl p-5 flex gap-4 items-start" style="background: var(--card); border: 1px solid var(--border); box-shadow: var(--shadow-xs)">
            <span class="inline-flex items-center justify-center min-w-12 h-11 rounded-lg text-base font-semibold shrink-0" style="background: rgba(52,76,163,0.10); color: var(--hooklyne-blue)">1cr</span>
            <div class="flex-1 min-w-0">
              <div class="flex items-baseline justify-between gap-3 mb-1">
                <span class="text-[13.5px] font-semibold text-[var(--heading)]">Meeting prep brief</span>
                <span class="text-[10.5px] text-[var(--muted-foreground)] whitespace-nowrap">per brief</span>
              </div>
              <p class="text-[12px] text-[var(--muted-foreground)] leading-relaxed mb-3">Request from My Leads or the Meeting Prep tab. PDF brief in under 5 minutes: company overview, talking points, key people, recent news, and your prospect's role context.</p>
              <div class="text-[11px] text-[var(--muted-foreground)] pt-2 border-t border-[var(--border)] flex flex-wrap gap-x-4 gap-y-1">
                <span><strong class="text-[var(--heading)] font-semibold">Included</strong> on every plan</span>
                <span><strong class="text-[var(--heading)] font-semibold">Average use</strong> 5-20 / month</span>
              </div>
            </div>
          </div>
        </div>

        <!-- ─── STAGE 3 ─── -->
        <div class="flex items-center gap-3.5 mb-5">
          <span class="flex items-center justify-center size-9 rounded-full text-base font-bold text-white shrink-0" style="background: var(--hooklyne-navy)">3</span>
          <div>
            <p class="text-[15px] font-semibold text-[var(--heading)] leading-tight">When signals fire, outreach is ready</p>
            <p class="text-[12.5px] text-[var(--muted-foreground)] leading-snug mt-0.5">Tracked prospects with hot signals get auto-drafted email, LinkedIn, and call scripts. No extra credit - included in your signal tracking subscription.</p>
          </div>
        </div>

        <p class="text-[13px] text-[var(--muted-foreground)] leading-relaxed mb-3 max-w-3xl">
          <span class="font-semibold text-[var(--heading)]">No extra credit.</span> Tracked prospects with hot signals come with three drafts ready to copy.
        </p>
        <div class="grid gap-x-8 gap-y-4 sm:grid-cols-3 mb-12 max-w-4xl">
          {[
            ["Signal-based email", "Drafted around the specific signal - hire, news, funding, expansion - in NL or EN, your voice."],
            ["LinkedIn request", "Short connection note referencing the trigger. Under 300 characters, ready to copy."],
            ["Call script", "Opening line + pitch + 3 discovery questions. Fires automatically on high-score signals."],
          ].map(([t, d]) => (
            <div>
              <p class="text-[13px] font-semibold text-[var(--heading)] mb-1">{t}</p>
              <p class="text-[12px] text-[var(--muted-foreground)] leading-relaxed">{d}</p>
            </div>
          ))}
        </div>

        <!-- ─── ALWAYS FREE ─── -->
        <div class="flex items-center gap-3.5 mb-5">
          <span class="flex items-center justify-center size-9 rounded-full text-base font-bold text-white shrink-0" style="background: #0F6E56">✓</span>
          <div>
            <p class="text-[15px] font-semibold text-[var(--heading)] leading-tight">Always free</p>
            <p class="text-[12.5px] text-[var(--muted-foreground)] leading-snug mt-0.5">Things you'll do every day that never cost a credit.</p>
          </div>
        </div>

        <div class="rounded-2xl overflow-hidden mb-4" style="background: var(--card); border: 1px solid var(--border)">
          <table class="w-full text-sm">
            <tbody>
              {[
                ["Browsing My Leads", "View, sort, filter, edit your pipeline"],
                ["Reading signals", "Open and read any signal feed at any time"],
                ["Weekly sector briefing", "5-10 articles from your market, scored for relevance"],
                ["Match to prospects", "See which of your tracked companies match the latest sector news"],
                ["Export to CSV", "Full export of your leads, signals, and outreach drafts"],
                ["View matched roles", "See up to 3 decision-makers per company before committing credits"],
              ].map(([k, v]) => (
                <tr class="border-b border-[var(--border)] last:border-0">
                  <td class="px-5 py-3 text-[12.5px] font-medium text-[var(--foreground)] whitespace-nowrap">{k}</td>
                  <td class="px-5 py-3 text-[12.5px] text-[var(--muted-foreground)]">{v}</td>
                </tr>
              ))}
            </tbody>
          </table>
        </div>

        <p class="text-[12px] text-[var(--muted-foreground)] leading-relaxed">
          <span class="font-semibold text-[var(--heading)]">Top-up any time:</span> add credits at €0.85 each, any plan, any amount. No minimum. Top-up credits roll forward (max 1× your plan quota).
        </p>
      </div>

      <!-- ===========================================================
           WHAT A TYPICAL MONTH LOOKS LIKE - compressed 3-card row
           =========================================================== -->
      <div class="mb-16">
        <h2 class="text-2xl md:text-3xl font-semibold text-[var(--heading)] tracking-tight mb-6">What a typical month looks like</h2>
        <div class="grid gap-3 md:grid-cols-3">
          {[
            { tier: "Start · 100 credits", body: "≈ 25 prospects · 10 tracked · 5 briefs", footer: "€1.56 per ready-to-send prospect", featured: false },
            { tier: "Growth · 400 credits", body: "≈ 90 prospects · 30 tracked · 10 briefs", footer: "€1.43 per ready-to-send prospect", featured: true },
            { tier: "Scale · 800 credits", body: "≈ 190 prospects · 60 tracked · 20 briefs", footer: "€1.26 per ready-to-send prospect", featured: false },
          ].map((u) => (
            <div class:list={["rounded-xl p-4", u.featured ? "border-2" : "border"]} style={u.featured ? "background: var(--card); border-color: var(--hooklyne-blue); box-shadow: var(--shadow-sm)" : "background: var(--card); border-color: var(--border); box-shadow: var(--shadow-xs)"}>
              <p class:list={["text-[10px] font-semibold uppercase tracking-[0.12em] mb-2", u.featured ? "text-[var(--hooklyne-blue)]" : "text-[var(--muted-foreground)]"]}>{u.tier}</p>
              <p class="text-[13px] text-[var(--heading)] mb-2 leading-relaxed">{u.body}</p>
              <p class="text-[11.5px] text-[#0F6E56]">{u.footer}</p>
            </div>
          ))}
        </div>
      </div>

      <!-- FAQ section -->
      <div class="mb-12">
        <div class="text-center mb-8">
          <h2 class="text-2xl font-semibold text-[var(--heading)] tracking-tight">Pricing questions.</h2>
          <p class="text-sm text-[var(--muted-foreground)] mt-2">Everything you need to know before you start.</p>
        </div>

        <div class="max-w-3xl mx-auto space-y-2" id="pricing-faq">
          {[
            {
              q: "What happens to unused credits at the end of the month?",
              a: "They roll forward for one month. The maximum balance you can carry is 2× your monthly quota - so if you have 400 credits a month on Growth, you can stack up to 800. Beyond that, the oldest credits expire. Most tools in this category expire credits monthly. We don't."
            },
            {
              q: "How does Route 1 (Find me companies) work in detail?",
              a: "One discovery costs 1 credit and returns up to 20 ranked companies matching your ICP. You then choose which to convert into prospect packages. Each conversion costs 3 more credits (package + contact). If you build all 20, the total is 61 credits. If you build 5, it's 16 credits. Companies you don't convert remain visible in your discovery for the session."
            },
            {
              q: "Why is the company phone included in contact finding?",
              a: "Because we found it costs us almost nothing to deliver, and it materially improves what you can do with each prospect. Other tools charge separately for it. We just include it."
            },
            {
              q: "Can I add more seats to my plan?",
              a: "On Growth, yes. You can add up to 2 extra seats at €30/seat/mo (so up to 4 seats total). Beyond that, you'll move to Scale, which already includes 5 seats and gives you 4× more credits. Start is solo-rep only - if you need a second seat, you're better off on Growth where you also get more credits, signals, and a setup call. Scale and Enterprise have fixed seat counts; for more seats on those tiers, talk to us."
            },
            {
              q: "What is a credit?",
              a: "A credit is the unit you spend on actions that produce a deliverable. Company discovery costs 1cr (up to 20 ranked companies). A prospect package costs 1cr (news-hooked email + LinkedIn message in your voice). Contact finding costs 2cr (verified email, triple-checked). Signal tracking costs 2cr per company per month. Browsing, reading intel, and exporting are always free."
            },
            {
              q: "What happens when I run out of credits?",
              a: "You can top up at any time at €0.85 per credit. There are no overage blockers - you just buy more credits when you need them. Your existing data, signals, and contacts remain accessible regardless of credit balance."
            },
            {
              q: "What is signal tracking?",
              a: "Signal tracking monitors a company daily for relevant events: job postings, news, funding rounds, leadership changes, and more. Each signal is scored for relevance to what you sell. Start includes up to 10 companies, Growth up to 40, Scale up to 100. Enterprise is negotiated. Additional companies above your limit cost 2cr per company per month."
            },
            {
              q: "Can I switch plans?",
              a: "Yes, anytime. Monthly plans switch at the next billing date. If you upgrade mid-month, you get prorated credit for the days remaining. Downgrading takes effect at your next renewal."
            },
            {
              q: "What is included in the free trial?",
              a: "10 full prospect packages. 1 seat configured with your ICP. Sector news for 2 weeks. Full portal access. Signal tracking, discovery, and meeting prep are visible but locked. We ask for a 20-min feedback call and case study permission in return. No payment needed."
            },
            {
              q: "Is there a contract?",
              a: "No contract on monthly plans - cancel anytime. Annual plans are a 12-month commitment billed upfront or monthly, with a discount built in. You save up to €60/mo on the Scale plan compared to monthly billing."
            },
            {
              q: "What does 'outreach in your voice' mean?",
              a: "During setup we collect writing samples and tone inputs from you. Every email and LinkedIn message Hooklyne generates is written to match your style, not a generic template. The prospect package is ready to send or copy-paste directly."
            },
            {
              q: "What does the 45-min setup call include on Growth?",
              a: "We configure your ICP with you, set up signal tracking for your target market, review your tone profile, and plan the first month of outbound. You leave the call with a working system and a list of first targets already queued."
            },
          ].map((item, i) => (
            <div class="faq-item rounded-xl bg-[var(--card)]" style="box-shadow: var(--shadow-xs)">
              <button
                class="w-full flex items-center justify-between px-5 py-4 text-left text-sm font-semibold text-[var(--heading)] hover:text-[var(--hooklyne-blue)] transition-colors"
                onclick={`toggleFaq(${i})`}
                aria-expanded="false"
                id={`faq-btn-${i}`}
              >
                {item.q}
                <svg class="size-4 shrink-0 ml-3 transition-transform duration-200 faq-icon" id={`faq-icon-${i}`} viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="m6 9 6 6 6-6"/></svg>
              </button>
              <div class="faq-answer" id={`faq-answer-${i}`}>
                <div>
                  <p class="px-5 pb-4 text-sm text-[var(--muted-foreground)] leading-relaxed">{item.a}</p>
                </div>
              </div>
            </div>
          ))}
        </div>
      </div>

    </div>
  </section>
  </Background>
</DefaultLayout>

<script>
  /* ── Billing toggle ─────────────────────────────────────────────── */
  let isAnnual = true;

  function toggleBilling() {
    isAnnual = !isAnnual;
    const knob = document.getElementById('toggle-knob') as HTMLElement;
    const labelMonthly = document.getElementById('label-monthly') as HTMLElement;
    const labelAnnual = document.getElementById('label-annual') as HTMLElement;
    const priceEls = document.querySelectorAll<HTMLElement>('.price-display');
    const saveNotes = document.querySelectorAll<HTMLElement>('.save-note');

    knob.style.transform = isAnnual ? 'translateX(24px)' : 'translateX(0)';
    labelMonthly.style.color = isAnnual ? 'var(--muted-foreground)' : 'var(--heading)';
    labelAnnual.style.color = isAnnual ? 'var(--heading)' : 'var(--muted-foreground)';

    priceEls.forEach(el => {
      const val = isAnnual ? el.dataset.annual : el.dataset.monthly;
      if (val) el.textContent = '€' + val;
    });

    saveNotes.forEach(el => {
      el.style.display = isAnnual ? '' : 'none';
    });
  }

  (window as any).toggleBilling = toggleBilling;

  /* ── Pricing FAQ accordion ──────────────────────────────────────── */
  function toggleFaq(index: number) {
    const answer = document.getElementById('faq-answer-' + index);
    const icon = document.getElementById('faq-icon-' + index);
    const btn = document.getElementById('faq-btn-' + index);
    if (!answer || !icon || !btn) return;

    const isOpen = answer.classList.contains('open');

    // Close all others
    document.querySelectorAll('.faq-answer').forEach((el, i) => {
      el.classList.remove('open');
      const ic = document.getElementById('faq-icon-' + i);
      const bt = document.getElementById('faq-btn-' + i);
      if (ic) ic.style.transform = '';
      if (bt) bt.setAttribute('aria-expanded', 'false');
    });

    if (!isOpen) {
      answer.classList.add('open');
      icon.style.transform = 'rotate(180deg)';
      btn.setAttribute('aria-expanded', 'true');
    }
  }

  (window as any).toggleFaq = toggleFaq;
</script>
